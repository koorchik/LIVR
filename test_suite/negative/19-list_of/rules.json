{
    "product_ids1": ["required", { "list_of": [[
        "required",
        "positive_integer",
        { "max_number": 100 }
    ]] }],
    "product_ids2": ["required", { "list_of": [
        "required",
        "positive_integer",
        { "max_number": 100 }
    ] }],
    "product_ids3": ["required", { "list_of": "positive_integer" } ],
    "user_ids": { "list_of": [[ "required" ]] }
}